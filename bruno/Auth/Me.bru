meta {
  name: Me
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/api/auth/me
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

docs {
  Protected endpoint — run Sign Up or Sign In first to populate {{accessToken}}.
}
